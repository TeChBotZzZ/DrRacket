#lang racket
(define (interpreter)
  (display "uofl> ")
  (let ((input (read)))
    
    (let ((result (eval input)))
      (display result)
      (newline))))

;type (intVal a' 10)
(define (intVal varName val)
  (define intval val)
  (let ((newVarName intval))
    newVarName))